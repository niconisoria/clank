[project]
name = "clank-dev"
version = "0.1.0"
description = "Dev tooling for Clank: hook tests + skill evaluator"
requires-python = ">=3.10"
dependencies = [
    "anthropic",
    "python-dotenv",
    "pydantic",
]

[dependency-groups]
dev = [
    "pytest",
]
