[project]
name = "i-dunno-dev"
version = "0.1.0"
description = "Dev tooling for i-dunno: hook tests + skill evaluator"
requires-python = ">=3.10"
dependencies = [
    "anthropic",
    "python-dotenv",
    "pydantic",
]

[dependency-groups]
dev = [
    "pytest",
]
